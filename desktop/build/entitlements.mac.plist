<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Hardened runtime entitlements for the signed, notarized build (#95).
       Electron needs JIT and unsigned executable memory for V8; the engine
       (PyInstaller) dlopens its own libraries, all signed with the same
       Developer ID, so library validation could stay on - it is disabled
       only because ctypes-loaded system libraries (PortAudio via
       sounddevice) are not ours to sign. The microphone entitlement is
       what lets the app ask for audio input at all. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.device.audio-input</key>
  <true/>
</dict>
</plist>
