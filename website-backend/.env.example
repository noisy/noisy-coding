# xAI API key used ONLY to mint ephemeral demo tokens. Env only - never
# commit a value, never log it. Name only in this example on purpose.
XAI_DEMO_API_KEY=

# Optional overrides (defaults in src/server.ts):
# DEMO_BACKEND_PORT=8788
# DEMO_ALLOWED_ORIGINS=http://localhost:5199
# DEMO_PAUSED=0
